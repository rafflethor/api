version: 0.1.0

database:
  driverClassName: org.postgresql.Driver
  url: jdbc:postgresql://localhost:5432/rafflethor
  username: "rafflethor"
  password: "rafflethor"

twitter:
  consumerKey:
  consumerSecret:
  accessToken:
  accessTokenSecret:

flyway:
  migrations:
    - /migrations
  schemas:
    - public
